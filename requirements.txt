# LocalConvert - Python package requirements
# External executables (LibreOffice, Pandoc, Calibre, FFmpeg, Tesseract,
# ImageMagick) are NOT pip packages - see README.md "External tool
# installation" section for platform-specific install instructions.

PySide6>=6.5,<7.0
PyMuPDF>=1.23
Pillow>=10.0
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
EbookLib>=0.18
beautifulsoup4>=4.12
pytesseract>=0.3.10

# Development / testing / packaging
pytest>=8.0
pyinstaller>=6.3
